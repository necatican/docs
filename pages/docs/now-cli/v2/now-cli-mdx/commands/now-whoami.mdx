import Snippet from '~/components/snippet'
import Caption from '~/components/text/caption'
import { InlineCode } from '~/components/text/code'

export const meta = {
  editUrl: 'pages/docs/now-cli/v2/now-cli-mdx/commands/now-whoami.mdx',
  lastEdited: '2019-09-03T21:19:41.000Z'
}

## Whoami

The `now whoami` command is used to show the username of the user currently logged into [Now CLI](/download).

### Basic Usage

<Snippet dark text="now whoami" />
<Caption>Using the <InlineCode>now whoami</InlineCode> command to view the username of the user currently logged into Now CLI.</Caption>

### Options Available

The following options can be passed when using the `now secrets` command:

- `--debug`
- `--global-config`
- `--help`
- `--local-config`
- `--token`

For more information on options and their usage, refer to the [options section](#options).
